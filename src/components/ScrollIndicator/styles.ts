import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  scrollIndicator: {
    position: 'absolute',
    backgroundColor: 'black',
    bottom: 0,
    left: 0,
    height: 2,
  },
});
